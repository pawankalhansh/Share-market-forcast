# Project Workspace

This workspace contains multiple projects.

## Subfolders

- `market-oracle/` — Market Oracle forecast project, including the static page and proxy server.
- `src/`, `public/`, and the Vite config files belong to the existing React calculator app.

## Notes

- To work on the Market Oracle project, open `market-oracle/`.
- The Market Oracle documentation is inside `market-oracle/README.md`.
